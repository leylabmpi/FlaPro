# final files generated by the pipeline
def which_input(wildcards):
    """
    Selecting input files for all rule
    """
    F = []
    # ShortBRED_quantify
    if (is_true(config['run_pipeline_steps']['alpha_div'])):
        #F.append(qc3c_dir + 'qc3c.done')
        ####F += expand(qc3c_dir + 'by-sample/{sample}.qc3C.html', sample = config['samples_unique'])
        #F += expand(rc_dir + "{sample}.txt", sample = config['samples_unique'])
        #F += expand(sbq_dir + "{sample}.txt", sample = config['samples_unique'])
        #F = rc_dir + 'merged_realcounts.txt'
        #F = rc_dir + 'psq.RData'
        F = div_dir + 'alpha_div.txt'
    # ret
    return F